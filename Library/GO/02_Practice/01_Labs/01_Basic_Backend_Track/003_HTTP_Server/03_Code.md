# Lab 003 - HTTP Server — Code

## Назначение

Здесь фиксируются ключевые фрагменты уже написанного HTTP-сервера и объяснение роли handler-ов.

## Файлы проекта

| Файл | Назначение |
|---|---|
| `main.go` | запуск сервера и регистрация маршрутов |
| `go.mod` | описание Go-модуля |

## Ключевые элементы

- создание handler-а;
- регистрация маршрута;
- запуск сервера;
- запись HTTP-ответа;
- обработка ошибки запуска.

## Место для фрагментов

```go
// Вставить ключевые фрагменты после самостоятельной реализации.
```

## Что объяснить после выполнения

- чем handler отличается от обычной функции;
- зачем нужен `ResponseWriter`;
- почему запуск сервера возвращает ошибку.
