# SQL Basics

Каноническая связанная заметка: [[01_Theory/06_Databases/CRUD_SQL]].

## Кратко

SQL используется для создания, чтения, обновления и удаления данных в реляционной базе. Для backend-курса минимум — понимать `SELECT`, `INSERT`, `UPDATE`, `DELETE`, `WHERE`, `JOIN`, `ORDER BY`, `LIMIT`.

